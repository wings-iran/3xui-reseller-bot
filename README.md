# ربات تلگرام مدیریت پنل 3X-UI 🤖

ربات تلگرام هوشمند برای مدیریت پنل 3X-UI با قابلیت‌های پیشرفته

## ✨ قابلیت‌ها

- 🔐 **مدیریت کاربران:** تنظیم حد ترافیک، بلاک/آنبلاک کاربران
- 📊 **مدیریت کانفیگ:** ساخت، حذف، تمدید کانفیگ‌ها
- ⏰ **چک خودکار ترافیک:** هر ۹ ساعت ترافیک کاربران را چک می‌کند
- ⚠️ **هشدار هوشمند:** اطلاع‌رسانی نزدیک شدن به حد مجاز
- 🔄 **همگام‌سازی:** بروزرسانی دستی ترافیک از پنل
- 📱 **رابط کاربری:** کیبوردهای شیشه‌ای تعاملی

## 🚀 نصب آسان

### روش ۱: نصب خودکار (توصیه شده)

```bash
# کلون کردن رپوزیتوری
git clone https://github.com/your-username/3xui-bot.git
cd 3xui-reseller-bot

# اجرای اسکریپت نصب
./install.sh
```

یا اگر قبلاً پوشه را دارید:

```bash
cd 3xui-reseller-bot
./install.sh
```

### روش ۲: نصب دستی

```bash
# کلون کردن رپوزیتوری
git clone https://github.com/your-username/3xui-bot.git
cd 3xui-reseller-bot

# نصب وابستگی‌ها
pip3 install -r requirements.txt

# تنظیم فایل config
cp config/config.py config/local.py
nano config/local.py

# اجرای ربات
python3 bot.py
```

## 📋 تنظیمات مورد نیاز

اسکریپت نصب از شما این اطلاعات را می‌پرسد:

| تنظیم | توضیح | مثال |
|-------|--------|------|
| **توکن ربات** | توکن دریافتی از @BotFather | `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11` |
| **آیدی ادمین** | آیدی عددی تلگرام ادمین | `123456789` |
| **آدرس پنل** | آدرس کامل پنل ۳X-UI | `https://panel.example.com:2053` |
| **نام کاربری** | نام کاربری پنل | `admin` |
| **رمز عبور** | رمز عبور پنل | `YourPassword123` |

## 🎛️ قابلیت‌های ادمین

- 👥 **مدیریت کاربران:** اضافه کردن/حذف کاربران مجاز
- 🚫 **بلاک کاربران:** مسدود کردن دسترسی کاربران
- 📈 **آمار کلی:** مشاهده مصرف کل، تعداد کانفیگ‌ها
- 🔄 **همگام‌سازی:** بروزرسانی دستی ترافیک همه کاربران
- ⚙️ **تنظیم حد ترافیک:** تغییر حد مجاز هر کاربر

## 👤 قابلیت‌های کاربر

- ➕ **ساخت کانفیگ:** انتخاب حجم و زمان دلخواه
- 📋 **مشاهده کانفیگ‌ها:** لیست کانفیگ‌های فعال
- 📄 **جزئیات کانفیگ:** مصرف، باقیمانده، زمان انقضا
- ⏰ **تمدید کانفیگ:** اضافه کردن حجم و زمان
- 🗑️ **حذف کانفیگ:** حذف کانفیگ بدون کسر از مصرف کل

## 🔧 مدیریت سرویس

```bash
# وضعیت ربات
sudo systemctl status 3xui-bot

# ریستارت ربات
sudo systemctl restart 3xui-bot

# توقف ربات
sudo systemctl stop 3xui-bot

# مشاهده لاگ زنده
sudo journalctl -u 3xui-bot -f

# مشاهده لاگ اخیر
sudo journalctl -u 3xui-bot -n 50
```

## 🛠️ نیازمندی‌ها

- Python 3.8+
- دسترسی sudo (برای نصب سرویس)
- پنل ۳X-UI فعال و قابل دسترسی

## 📦 وابستگی‌های پایتون

- `python-telegram-bot` - رابط تلگرام
- `aiohttp` - ارتباط با API پنل
- `aiosqlite` - دیتابیس محلی
- `apscheduler` - زمان‌بندی وظایف

## 🚨 عیب‌یابی

### ربات اجرا نمی‌شود
```bash
# چک کردن لاگ‌ها
journalctl -u 3xui-bot -n 20

# اجرای دستی برای تست
cd /root/3xui-reseller-bot
python3 bot.py
```

### مشکل اتصال به پنل
- آدرس پنل را چک کنید
- پورت پنل باز باشد
- نام کاربری و رمز عبور صحیح باشد

### مشکل توکن ربات
- توکن را از @BotFather دوباره دریافت کنید
- مطمئن شوید ربات بلاک نشده

## 📝 لاگ‌ها

لاگ‌های ربات در journal ذخیره می‌شوند:

```bash
# لاگ یک ساعت اخیر
journalctl -u 3xui-bot --since "1 hour ago"

# لاگ امروز
journalctl -u 3xui-bot --since today

# دنبال کردن لاگ زنده
journalctl -u 3xui-bot -f
```

## 🤝 مشارکت

برای مشارکت در توسعه:

1. Fork پروژه
2. ایجاد branch جدید
3. اعمال تغییرات
4. ارسال Pull Request

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 👨‍💻 توسعه‌دهنده

ساخته شده با ❤️ توسط [@wingsbotCr](https://t.me/wingsbotCr)
