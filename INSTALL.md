# 📦 راهنمای نصب ربات تلگرام مدیریت پنل 3X-UI

## 🚀 نصب سریع (توصیه شده)

```bash
# ۱. کلون کردن رپوزیتوری
git clone https://github.com/your-username/3xui-bot.git
cd 3xui-bot

# ۲. اجرای اسکریپت نصب
./install.sh
```

## 📋 فایل‌های موجود

پس از کلون کردن، این فایل‌ها در پوشه `3xui-bot` موجود هستند:

| فایل | توضیح |
|------|--------|
| `install.sh` | **اسکریپت نصب خودکار** ✨ |
| `check-requirements.sh` | چک نیازمندی‌های سیستم |
| `README.md` | راهنمای کامل |
| `PANEL_COMPATIBILITY.md` | سازگاری با پنل‌ها |
| `test-panel-compatibility.py` | تست سازگاری |
| `demo-install.txt` | نمونه نصب |

## 🔧 مراحل نصب دستی

اگر نمی‌خواهید از اسکریپت خودکار استفاده کنید:

```bash
# ۱. کلون کردن
git clone https://github.com/your-username/3xui-bot.git
cd 3xui-bot

# ۲. تنظیم فایل config.py
nano config.py

# ۳. نصب وابستگی‌ها
pip3 install -r requirements.txt

# ۴. اجرای دستی برای تست
python3 bot.py
```

## ⚙️ تنظیمات config.py

فایل `config.py` را با اطلاعات خود ویرایش کنید:

```python
BOT_TOKEN = "توکن_ربات_تلگرام"
SUDO_ADMIN_ID = آیدی_عددی_ادمین
PANEL_URL = "https://your-panel.com:2053"
PANEL_USERNAME = "admin"
PANEL_PASSWORD = "رمز_عبور_پنل"
```

## 🔍 تست نصب

### ۱. چک نیازمندی‌ها
```bash
./check-requirements.sh
```

### ۲. تست سازگاری پنل
```bash
python3 test-panel-compatibility.py
```

### ۳. اجرای آزمایشی ربات
```bash
python3 bot.py
```

## 🌐 تنظیمات پنل 3X-UI

ربات با این تنظیمات پنل سازگار است:

- **پروتکل‌ها**: VLESS, VMess, Trojan
- **امنیت**: Reality, TLS, none
- **شبکه**: TCP, WebSocket, gRPC, HTTP/2
- **نسخه پنل**: 3X-UI v1.x, v2.x

## 🔑 اطلاعات مورد نیاز

| اطلاعات | نحوه دریافت |
|---------|-------------|
| **توکن ربات** | از [@BotFather](https://t.me/BotFather) |
| **آیدی ادمین** | از [@userinfobot](https://t.me/userinfobot) |
| **آدرس پنل** | آدرس کامل پنل 3X-UI شما |
| **رمز پنل** | رمز عبور پنل مدیریت |

## 📱 دسترسی‌های مورد نیاز

ربات نیاز به این دسترسی‌ها دارد:

- ✅ دسترسی اینترنت
- ✅ دسترسی به پنل 3X-UI
- ✅ دسترسی sudo برای نصب سرویس
- ✅ دسترسی به پورت 443 (یا پورت پنل)

## 🎯 نحوه استفاده بعد از نصب

### شروع ربات
```bash
sudo systemctl start 3xui-bot
```

### وضعیت ربات
```bash
sudo systemctl status 3xui-bot
```

### مشاهده لاگ
```bash
sudo journalctl -u 3xui-bot -f
```

### ریستارت ربات
```bash
sudo systemctl restart 3xui-bot
```

## 🚨 عیب‌یابی

### ربات اجرا نمی‌شود
```bash
# چک لاگ‌ها
sudo journalctl -u 3xui-bot -n 20

# اجرای دستی برای تست
cd /root/3xui-bot
python3 bot.py
```

### مشکل اتصال به پنل
- آدرس پنل را چک کنید
- پورت پنل باز باشد
- نام کاربری و رمز عبور صحیح باشد

### مشکل توکن ربات
- توکن را از @BotFather دوباره دریافت کنید
- مطمئن شوید ربات بلاک نشده

## 📞 پشتیبانی

اگر مشکل دارید:

1. `./check-requirements.sh` را اجرا کنید
2. `python3 test-panel-compatibility.py` را تست کنید
3. لاگ‌های ربات را چک کنید: `sudo journalctl -u 3xui-bot -n 50`

## ✅ نتیجه

پس از نصب موفق، ربات:
- 🔄 به صورت خودکار اجرا می‌شود
- 📊 هر ۹ ساعت ترافیک را چک می‌کند
- ⚠️ هشدار مصرف را ارسال می‌کند
- 🔧 قابل مدیریت از طریق systemctl است